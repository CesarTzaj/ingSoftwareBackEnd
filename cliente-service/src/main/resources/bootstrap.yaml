spring:
  application:
    name: cliente-service
  datasource:
    url: jdbc:mysql://localhost:3306/ejercicio
    username: root
    password: 
  cloud:
    config:
      enable: true
      uri: http://localhost:8081